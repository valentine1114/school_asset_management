<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.sspku.assets.mapper.AdminAssetMapper">
    <select id="getByAssetName" parameterType="string" resultType="cn.sspku.assets.entity.AdminAsset">
        SELECT * FROM a_admin_asset WHERE name = #{name}
    </select>

</mapper>
